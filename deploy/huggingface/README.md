---
title: D2D Docs To Dark
emoji: 🌙
colorFrom: gray
colorTo: indigo
sdk: docker
app_port: 1313
pinned: false
---

# D2D — Docs to Dark (API)

FastAPI service that inverts PDFs and images into dark mode.
Built from the repo root `Dockerfile` (`pip install .[server]`).

Interactive docs: `/docs` · health: `/`

This README's front matter is what Hugging Face reads to configure the Space —
copy it to the **root `README.md` of the Space repo** (see `deploy/DEPLOY.md`).
